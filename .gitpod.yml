# .gitpod.yml
tasks:
  - init: npm install -g serve   # install a lightweight static server
  - command: serve .             # serve your project files

ports:
  - port: 3000
    onOpen: open-browser         # open in a full browser tab instead of Gitpod preview
